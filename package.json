{
  "name": "i-ching-web",
  "version": "1.0.0",
  "description": "I Ching oracle web app - React + Express + SQLite",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "vite",
    "dev:server": "node server/index.js",
    "build": "vite build",
    "postinstall": "node scripts/postinstall.cjs",
    "start": "NODE_ENV=production node server/index.js",
    "preview": "vite preview",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "sql.js": "^1.12.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
